/**
 * Command palette public barrel.
 *
 * Usage:
 *   import { CommandSurface } from "@/components/command";
 *
 * Mount <CommandSurface /> once near the app root (inside <ShortcutProvider>).
 * It registers seed commands and handles Cmd+K globally.
 */
export { CommandSurface } from "./CommandSurface";
export { CommandPalette } from "./CommandPalette";
